import { SimpleListScreen } from "@/utils/uiHelper";

export default function ArchitectPage() {
  return <SimpleListScreen title="Architect coordination" items={["Sketch pending", "Follow-up due", "Revised concept uploaded"]} />;
}